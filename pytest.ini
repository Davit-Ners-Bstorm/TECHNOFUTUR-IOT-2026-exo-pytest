[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: fast isolated business-rule tests
    integration: tests involving several project components
    hardware: demonstrations using simulated hardware only
